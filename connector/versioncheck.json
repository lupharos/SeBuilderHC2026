{
    "productName": "HC Studio",
    "version": "2026.08.28.2",
    "releasedAt": "2026-08-28T01:00:00Z",
    "notes": "v2026.08.28.2: Fixed PowerPoint export 500 error — pptxgenjs 3.x's write() is async (returns a Promise) but the server route wasn't awaiting it, so Buffer.from() threw on every request. Route is now async and awaits write() correctly."
}
